# Security

## Official downloads

نسخه‌های رسمی PREEMAX NetMonitor فقط از این منابع منتشر می‌شوند:

- GitHub Releases همین مخزن
- وب‌سایت رسمی PREEMAX: https://preemax.ir

برای هر نسخه، در صورت ارائه `SHA256SUMS.txt` مقدار SHA‑256 فایل دانلودشده را با مقدار رسمی مقایسه کنید.

## Reporting a security issue

اگر مشکل امنیتی پیدا کردید، آن را به‌صورت عمومی همراه با اطلاعات حساس، کلیدها، رمزها یا داده‌های شخصی منتشر نکنید. برای ارتباط با ناشر از اطلاعات تماس موجود در وب‌سایت رسمی PREEMAX استفاده کنید.

## Digital signing

هر پلتفرم از سازوکار امضای دیجیتال استاندارد خودش استفاده می‌کند. وضعیت امضای هر فایل در Release مربوط به همان نسخه مشخص خواهد شد.
